export type UserLoginFormData = {
    email: string
    password: string
}

export type UserRegistrationFormData = {
    name: string
    email: string
    password: string
    confirmPassword: string
}